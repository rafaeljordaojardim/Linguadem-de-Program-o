#include <stdio.h>
#include <stdlib.h>

int main()
{
    char nome[51] = "andre";
    char outro[51];
    //outro = nome; you musn't use this
    int i;

   /* for(i=0; i <= strlen(nome);i++)
    {
        outro[i] = nome[i];
    }
    printf("%s",outro);*/

    return 0;
}
